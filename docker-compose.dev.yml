services:
  nginx:
    environment:
      - VIRTUAL_HOST=dev.qualilead.options.net
    volumes:
      - ./compose/dev/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./compose/dev/nginx/.htpasswd:/etc/nginx/.htpasswd

  certbot:
    environment:
      - HOST_PROD=dev.qualilead.options.net
      - EMAIL_ADMIN=admin.web@options.net